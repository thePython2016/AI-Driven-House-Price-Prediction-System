apiVersion: v1
kind: Service
metadata:
  name: house-prediction-model-service
spec:
  type: LoadBalancer
  selector:
    app: house-prediction-modeltext
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501